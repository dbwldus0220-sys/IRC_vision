# 라인 복귀 및 소실 탐색 수정 — 2026-09-20

- RECOVER 명령의 첫 LEFT/RIGHT(라인 위치)와 관계없이 TURN 뒤의 회전 방향과 n을 그대로 매핑한다.
- 왼쪽 반복 수: 2/3/4/5/6/7/8/10/13. 오른쪽: 2/3/4/5/6/7/8/10/12/15.
- 데이터 출처: `/home/jet/Downloads/robot_motions-1(3).json`.
- 원본에 없는 반복 수는 같은 방향 4회 보행 주기에서 repeat_count만 변경했다.
  원본의 8회 오타 이름은 표준 이름으로 추가했다. 완료 허용 오차는 5°를 유지했다.
- 라인이 왼쪽으로 사라지면 `제자리좌회전45도-1(1회)`, 오른쪽이면 `제자리우회전45도-1(3회)`.
- 오른쪽 탐색 모션은 최신 첨부 JSON과 관절값·반복 수·속도가 일치하며, 완료 허용 오차 5°는 유지한다.
- 일반 라인 주행 중 최신 정상 검출점의 좌우 방향을 보행 중에도 기억한다.
  검출점이 없는 호환 입력은 lateral offset을 사용한다.
- 탐색 회전 완료 전 중복 명령을 차단하고, 완료 시 기존 라인 관측을 비워 새 영상으로 판단한다.
  미검출 지속 시 새 관측과 완료 확인마다 탐색을 반복하며, 재검출 시 일반 판단으로 복귀한다.
- 관측 자체가 없거나 오래됐거나 기억한 방향이 없으면 정지한다.
- 공/골대/허들 미션 우선순위 및 공 집기 후 전용 라인 정렬은 유지했다.
- 라인 소실은 이전 형상 기반으로 대기 중인 RECOVER보다 우선한다.
  오래된 보행 중 검출 프레임만으로 다음 STRAIGHT를 사전 예약하지 않는다.

## 검증

- 관련 테스트: 978 통과, 기존 문제 2 실패.
- 실패 1: 공 소실 복구 테스트의 BALL_APPROACH_TURN_LEFT_6 기대값과 WAIT의 불일치.
- 실패 2: 일반 좌/우회전실실전 12개 모션 누락. 이번 복귀/탐색 alias 21개는 모두 해석된다.
- 복귀 프레임 각도·토크·타이밍을 원본 데이터의 SHA256과 비교해 통과했다.
- git diff --check 통과.
- 로그: `/tmp/step-line-recovery-tests.log`
- 작업 전 백업: `/tmp/robot_motions.before-line-recovery-remap.json`
- 기존 복귀 모션 11개 갱신, 새 반복 수/표준 이름 8개 추가. 다른 기존 모션 객체는 동일하다.
- 실기 및 물리 시뮬레이션은 실행하지 않았다. 새 복귀 주기의 실제 회전량과
  오른쪽 탐색 종료 자세(오뒤415)에서 다음 동작 연결을 시뮬레이션 후 지지 상태에서 확인해야 한다.

## 갱신 모션

- 라인복귀우회전(2회)
- 라인복귀우회전(3회)
- 라인복귀우회전(4회)
- 라인복귀우회전(5회)
- 라인복귀우회전(6회)
- 라인복귀좌회전(2회)
- 라인복귀좌회전(3회)
- 라인복귀좌회전(4회)
- 라인복귀좌회전(5회)
- 라인복귀좌회전(6회)
- 라인복귀좌회전(7회)

## 추가 모션

- 라인복귀우회전(10회)
- 라인복귀우회전(12회)
- 라인복귀우회전(15회)
- 라인복귀우회전(7회)
- 라인복귀우회전(8회)
- 라인복귀좌회전(10회)
- 라인복귀좌회전(13회)
- 라인복귀좌회전(8회)

## 남아 있는 일반 회전 모션 누락

- 우회전실실전(10회)
- 우회전실실전(12회)
- 우회전실실전(15회)
- 우회전실실전(4회)
- 우회전실실전(6회)
- 우회전실실전(8회)
- 좌회전실실전(10회)
- 좌회전실실전(12회)
- 좌회전실실전(15회)
- 좌회전실실전(4회)
- 좌회전실실전(6회)
- 좌회전실실전(8회)
