# mission_control

`mission_control`은 STEP의 비전 결과를 action으로 결정하고, 그 action을 C++
motion executor 요청으로 연결하는 ROS 2 패키지다. 모든 기능을 하나의
`main.cpp`로 합치지 않으며 `full_system.launch.py`를 안전한 개발 환경의 공식
실행 진입점으로 사용한다.

## 공식 실행 구조

```text
step/yolo26_detector
  -> step/unified_vision_node
  -> mission_control/motion_decision_node
  -> mission_control/motion_command_bridge_node
  -> irc_step_motion_executor/sdk_motion_executor
```

실제 모터 제어를 소유할 수 있는 노드는 `sdk_motion_executor` 하나뿐이다. 같은
ROS graph에서 Python executor나 다른 motor executor를 동시에 실행하면 안 된다.

## 모듈 책임

- `step`
  - 카메라 입력을 처리하고 비전 결과를 생성한다.
  - 모터와 RobotMotionPlayer SDK에 직접 접근하지 않는다.
- `mission_control/motion_decision_node`
  - 비전 결과를 바탕으로 수행할 action을 결정한다.
  - SDK `motion_id`를 직접 실행하지 않고 bridge에 action을 전달한다.
- `mission_control/motion_command_bridge_node`
  - 지원되는 action을 현재 catalog의 motion alias로 변환한다.
  - `request_id`, command/event ID와 executor status의 상관관계를 관리한다.
  - 모터나 외부 SDK에 직접 접근하지 않는다.
- `irc_step_motion_executor/sdk_motion_executor`
  - 실제 SDK 실행의 유일한 소유자다.
  - hardware enable과 torque approval 정책을 검증한다.
  - 실행 상태와 오류를 `/motion/executor/status`로 반환한다.

현재 bridge는 `STRAIGHT`를 `line_forward_6`, `APPROACH`를 `forward`, `PICKUP_NOW`를 `pickup`,
`GO`를 `hurdle`로만 연결한다. 짧은 전진·회전·SHOT처럼 실물 검증된 SDK motion이
없는 action은 executor로 전송하지 않고 `/motion/status`에 `UNSUPPORTED`를
발행한다.

## 공식 실행 명령

안전한 개발 실행은 simulated backend만 사용한다.

```bash
ros2 launch mission_control full_system.launch.py \
  enable_camera:=false display:=false
```

이 launch는 다음 값을 코드에서 고정한다.

- `backend_type=simulated`
- `enable_robot_hardware=false`
- `explicit_torque_approval=false`

향후 실물 실행의 진입점은 아래 파일이다. 아직 완성된 실물 실행 명령이나 실제
값 예시는 제공하지 않는다.

```text
ros2 launch mission_control full_system_robot.launch.py ...
```

`full_system_robot.launch.py`도 기본적으로 camera와 hardware를 비활성화하고,
torque 승인을 부여하지 않는다. 실물 실행 전에는 다음 항목을 모두 별도 안전
절차에서 검토하고 명시해야 한다.

- 승인된 SDK-enabled 빌드
- 기존 파일인 `motion_json_path`
- 승인된 `robot_device_path`
- 승인된 양의 `robot_baud_rate`
- 완전하고 중복 없는 `robot_motor_ids` 정수 배열
- `enable_robot_hardware=true`에 대한 별도 승인
- `explicit_torque_approval=true`에 대한 독립적인 torque 승인
- 로봇 지지, 비상정지 및 시뮬레이션 선행 검증

`enable_robot_hardware=false`인 기본 상태에서는 SDK runtime이나 Dynamixel 객체를
생성해서는 안 된다. 설정 validation 성공 또한 motion-ready를 의미하지 않는다.

## 백보드 정렬과 득점 (2026-09-20)

- 골대 안의 백보드를 우선하고, 없으면 단독 백보드를 사용한다. 골대만으로는
  추종하지 않는다. 중심축은 공과 같은 화면 중앙 +96px다.
- 백보드 깊이 검출·추적 상한은 **2.0m**다. 1.5~2.0m에서도 유효한 깊이를
  전달하며, 더 가까운 거리도 계속 검출한다. 골대 제어 범위도 **2.0m 이내**로
  설정하여 공 집기 → 라인 정렬 → 전진45도 6회 → 3초 정지·라인 재정렬 → 카메라90도
  전환 이후 아래 거리표로 접근한다.
- 득점 조건은 보정된 `offset_x_px`가 **-40~+100px**, `depth_m`이
  **0.77~0.82m**인 경우다. 양 끝값을 포함하며 기존 검출·깊이·득점 확정과
  공 잡기 확인 조건은 유지한다. 조건을 통과하면 **슛 직전 3초 정지** 후
  `goal_shot` → `골넣기실전`을 실행한다. 대기 중 조건이 깨지거나 관측이 오래되면
  대기를 취소하고, 조건을 다시 충족한 시점부터 3초를 센다. 일반 모션 완료 후
  3초 대기와는 별도로 적용한다.
- 0.82m 초과~2.0m에서는 꽃게걸음 없이 제자리 회전으로 방향을 보정한다.
  우측 오차는 기존대로 15° 이상이면 회전한다. 좌측은 골대 전용 기준으로
  15° 이하는 회전하지 않고, 15° 초과~45° 이하는 1회, 45° 초과부터는
  기존 횟수 구간을 사용한다. 회전하지 않는 범위에서는 좌우 픽셀 위치가
  중앙 범위를 벗어나도 거리별 전진을 이어간다.
  0.77~0.82m에서는 제자리 회전보다 픽셀 기준을 우선하며, -40px 미만이면
  왼쪽 게걸음, +100px 초과이면 오른쪽 게걸음으로 맞춘다.
- `전진실험45도(8회)`와 카메라90도 전환 완료 후, 그리고 이후 골대 일반 모션
  성공 완료마다 3초 정지한다. 정지 중 들어온 관측은 폐기하고 새 골대 관측으로
  판단한다. 기존 전진4회 이후의 라인 정렬 절차는 바꾸지 않는다.
- 실행기는 `post_ball_camera_90` 성공부터 `goal_shot` 성공까지 카메라90도를
  유지한다. 이 구간의 모든 모션에서 머리 모터 ID 0의 목표각을 **1.0°**로
  덮어쓴다. 이는 현재 JSON의 카메라90도 자세에 해당하는 모터각이며,
  `미세오뒤에서기본자세`와 `골넣기실전` 마지막 프레임에도 적용한다.
  기존 공 추적용 카메라0도 덮어쓰기는 이 구간에서 활성화하지 않는다.
  골넣기 실패·시간 초과 시에는 재시도를 위해 유지하며, 정상 완료·유효한 모션 취소·
  새 픽업 시작 시 해제한다. 해제 자체가 카메라 복귀 동작을 만들지는 않으므로,
  이후 실행하는 모션의 머리 목표각을 따른다. 실행기 재시작 시 유지 상태는 초기화된다.
  기본 설정은 `goal_head_override_enabled=true`, `goal_head_override_deg=1.0`이며,
  heartbeat의 `goal_head_override_active`로 적용 상태를 확인할 수 있다.
- `SHOT` 명령 전에는 일반 정지 설정과 별도로 항상 3초 정지한다.
  이번 골대 접근에서 `GOAL_CAMERA90_FINE_FORWARD_1~4`가 성공했고 좌우 꽃게걸음은
  한 번도 성공하지 않았다면, `SHOT`을 `미세오뒤에서기본자세` → 완료 후 2초 정지
  → `골넣기실전` 순서로 실행한다. 기존 명령 전 3초 정지는 자세 전환 전에 유지된다.
  꽃게걸음을 사용했거나 미세전진 이력이 없으면 기존 골넣기 모션을 바로 실행한다.
  자세 전환과 2초 정지 중에는 다른 명령을 끼워 넣지 않으며, 전환 실패·시간 초과·취소 시
  골넣기로 진행하지 않는다. 자세 전환 성공을 득점 성공으로 보고하지 않는다.
  이력은 새 픽업 시작, 카메라90도 전환 성공, 골넣기 성공 시 초기화한다.
- 접근 판단은 `ground_distance_m`이 아닌 **백보드 `depth_m`(Depth Z)**를 사용한다.
  목표 중심은 0.795m이며 허용 범위는 0.77~0.82m다. 다음 표의 거리는 목표까지의
  차이가 아니라 현재 측정한 Depth Z다. 실행 완료 후 다시 측정하여 다음 모션을 고른다.

  | Depth Z (mm) | 모션 | 명령 |
  | --- | --- | --- |
  | 1280 < Z ≤ 2000 | 전진실험90도(6회) | `GOAL_CAMERA_90_FORWARD` |
  | 1025 < Z ≤ 1280 | 전진실험90도(4회) | `GOAL_CAMERA_90_FORWARD_2` |
  | 985 < Z ≤ 1025 | 미세90도-4(4회) | `GOAL_CAMERA90_FINE_FORWARD_4` |
  | 950 < Z ≤ 985 | 미세90도-4(3회) | `GOAL_CAMERA90_FINE_FORWARD_3` |
  | 875 < Z ≤ 950 | 미세90도-4(2회) | `GOAL_CAMERA90_FINE_FORWARD_2` |
  | 820 < Z ≤ 875 | 미세90도-4(1회) | `GOAL_CAMERA90_FINE_FORWARD_1` |
  | 770 ≤ Z ≤ 820 | 전진 중단, 중앙 정렬 및 득점 안정 확인 | 게걸음 / 대기 / SHOT |
  | Z < 770 | 후진실전-2(1회) | `GOAL_CAMERA90_BACKWARD_1` |

  후진 모션은 `후진실전-2(3회)`의 프레임·속도·완료 조건을 그대로 복사하고
  `repeat_count`만 1로 바꾼 별도 데이터다. 원본 픽업 후진은 3회를 유지한다.
  후진 성공 후 3초 정지하고 새 백보드 깊이로 다시 판단한다. 여전히 0.77m 미만이면
  한 번 더 후진하며, 0.77~0.82m이면 좌우 정렬·득점 확인으로 넘어간다.
  0.82m를 넘으면 기존 전진 거리표를 적용한다. 골대 카메라90도 유지도 후진 중 적용된다.
  실제 1회 후진 거리가 5cm 목표 구간보다 클 수 있으므로, 실물에서는 지지 상태에서
  후진 이동량과 전진·후진 왕복 여부를 먼저 확인한다.

- 요청 표에서 같은 `STRAIGHT_4`가 여러 동작을 가리키므로, 실행 명령은 모션별로
  구분한다. LINE/BALL의 기존 STRAIGHT 매핑은 유지한다.
- `Downloads/robot_motions-1(3).json`의 `미세45도-4`, `미세0도-4`, `미세90도-4`
  및 골대용 1~4회 모션을 런타임에 등록했다. 기본 45도/0도 모션은 원본의 5회 반복이다.
  첨부 파일의 `미세90도-4(2회/3회/4회)`에는 공잡기 프레임이 잘못 들어 있어,
  정상 `미세90도-4(1회)`의 4프레임 보행 주기에 반복 횟수만 바꿔 등록했다.
  신규 모션의 완료 허용 오차는 기존 런타임 정책인 5°를 유지한다.
- `골넣기실전`은 `Downloads/robot_motions-1(1).json`의 동일 이름 모션만
  반영했다. 첫 두 프레임은 각각 600ms이며, 완료 허용 오차는 사용자 요청으로
  원본의 2° 대신 5°를 사용한다.

실물 실행 전 시뮬레이션에서 득점 경계값과 모션 완료 후 정지를 검증하고,
로봇을 지지한 상태에서 새 모션과 백보드 거리 기준을 확인해야 한다.

## Ball pickup 미세 접근

`공확인자세` 완료 후 기존 3초 확인 구간에서 수신한 최신 최대 40개 프레임 중
`grab` confidence가 0.25 이상인 프레임이 15개 이상이면 `GRABBED`로 확정한다.
연속 검출일 필요는 없고, 마지막 프레임이 미검출이어도 이 횟수를 만족하면 성공이다.
한 프레임에 여러 `grab` 검출이 있어도 성공 1개로 세며, 중복·역순 타임스탬프와
확인 시작 전 이미 수신한 프레임은 집계하지 않는다. 40개보다 많이 수신하면 오래된 프레임부터
제외한다. 3초 안에 40개 미만을 수신해도 최소 성공 횟수 15개는 그대로 적용한다.
새 프레임이 하나도 없으면 `UNKNOWN`, 프레임은 있지만 성공이 15개 미만이면
`NOT_GRABBED`다. 확인 종료 시 해당 공의 결과를 저장하며, 이후 영상으로 덮어쓰지 않는다.
`GRASP_VERIFY_END`와 판단 debug의 `decision_window_frames` 및
`decision_window_grabbed_frames`로 최종 판단에 사용한 프레임 수를 확인할 수 있다.
`accepted_frames`/`grabbed_frames`는 전체 확인 구간 통계이고,
`confidence`/`final_confidence`와 `last_fresh_frame_result`는 마지막 프레임 관측값이다.
이 기준은 일시적인 미검출에 더 관대하지만, 실제로 공을 놓친 경우에도 이전 성공
프레임이 15개 남아 있으면 성공으로 판정할 수 있다.

픽업 전 미세전진은 `미세0도-4`, 공 확인 후 후진은 `후진실전-2(3회)`이다.
570mm 이내 거리 판단으로 선택한 초기 접근 `미세0도-4`에는 준비 모션을 넣지 않는다.
이후 공잡기 전 미세 정렬 단계에서 `미세0도-4`를 요청할 때에는
`오뒤에서미세오뒤`를 먼저 1회 실행한다.
접근·보정 모션 완료와 3초 정지 후 새 영상에서 미세 정렬이 확인되면
추가 준비 자세·미세전진 없이 `공잡기전후진-2(2회)`로 바로 진행한다.
정렬이 완료되기 전에는 기존 미세전진·꽃게걸음 보정과 완료 후 재판단을 유지한다.
정렬 중 공이 미검출되면 현재 판단 로직은 재탐색하거나 대기한다.
브리지에는 `use_no_ball_pickup_path=true`용 별도 분기가 남아 있지만,
현재 판단 로직은 이 플래그를 생성하지 않는다. 이 분기는 준비 모션과 미세전진,
후진을 수행한 뒤 공 재검출·정렬 확인을 거쳐야 잡기로 진행한다.
준비 모션 성공 직후 미세전진을 시작하고, 미세전진 완료 후 기존 3초 대기와
새 영상 확인을 진행한다. 준비 모션 실패·시간 초과·취소 시에는 미세전진을
실행하지 않는다. 준비 모션의 마지막 자세는 원본의 `미세오뒤401(90도)`이므로
0도 미세전진으로 이어지는 관절·카메라 자세 변화는 지지 상태에서 확인해야 한다.
명령 ID `pickup_retreat_2`는 유지하며, `후진실전-2(3회)`의 `repeat_count`는 3으로 실제 후진은 3회 반복한다.
이 후진 모션은 `Downloads/robot_motions(18).json`에서 가져오고 완료 허용 오차만 5°로 적용했다.
공잡기 전 후진 `pickup_pre_backward_camera_down`은 `Downloads/robot_motions(15).json`의
`공잡기전후진-2(2회)`로 연결하며, `공잡기리그랩까지 실전`도 해당 파일의 데이터로 갱신했다.
오른쪽 꽃게는 공잡기에서 `미세오옆꽃게0도`, 골대에서 `미세오옆꽃게90도-1`을 사용한다.
왼쪽 꽃게는 `Downloads/robot_motions(18).json`의 `미세왼옆꽃게0도-1`(공잡기),
`미세왼옆꽃게90도-1`(골대)을 사용한다.
가져온 모션은 원본 프레임·시간·속도·반복 수를 유지하고 완료 허용 오차만 5°로 적용했다.
런타임 전체 모션의 `completion.position_tolerance_deg`도 5°로 통일한다.
라인 기본 전진은 `전진실험45도(6회)`, 일반 BALL 접근은 `전진실험45도(4회)`,
픽업 후 라인 정렬이 끝나면 기본 라인 제어기로 10초간 주행한다.
골대 일반 전진은 거리표에 따라 `전진실험90도(4회)` 또는 `전진실험90도(6회)`다.
이 전진 모션들은 `Downloads/robot_motions(21).json`에서 가져왔으며 완료 허용 오차만
5°로 적용했다. 각 모션은 2회 전진 분량의 8프레임으로 구성되어 있으며,
4/6/8회 모션의 `repeat_count`는 각각 2/3/4, `playback_speed`는 원본의 1.15다.
득점 후 라인 복귀 전진도
`line_forward_6` 별칭을 공유하므로 `전진실험45도(6회)`를 사용한다.
기존 전진과 관절 궤적·시간 설정이 다르므로 실제 연속 주행 전에 지지 상태에서 확인한다.
후진이 끝나면 첫 번째 공은 `후진하고 제자리우회전(공)`, 두 번째 공은
`후진에서 제자리좌회전(공)`을 실행하고 각각 3초 대기 후 픽업 시퀀스를 완료한다.
두 복합 모션은 `Downloads/robot_motions(19).json`에서 가져왔으며,
각각 `pickup_first_backward_turn_right`, `pickup_second_backward_turn_left` 별칭으로
1회만 실행한다. 첫 번째는 내부에 우회전 7회가 포함되어 있다.
두 번째는 설명 그림에는 좌회전 3회로 표시되어 있으나 첨부 JSON에는 4회가 들어 있어
JSON 원본의 10프레임을 유지했다. 별도의 회전 명령은 추가하지 않는다.
네 공·골대 복합 모션 모두 원본의 관절 각도·토크·프레임 시간·재생 속도를 유지하고
완료 허용 오차만 5°로 적용했다. 이후 `tools/upsert_motion_catalog.py`로 가져오는
모션과 기존 런타임 모션에도 5°를 적용한다. 이는 관절 도달 판정의 허용 오차이며
영상 기반 라인 방향 정렬의 각도 기준과는 별개다.

두 픽업 모두 이후 새 라인 영상으로 재정렬한다. 라인이 검출되지 않으면
첫 번째 공은 우회전 2회(약 15°), 두 번째 공은 좌회전 1회(약 30°)씩 탐색한다.
라인이 보이면 heading에 맞춰 보정하며, 보정 중 다시 놓치면 마지막 보정 방향으로
탐색한다. 회전 중 라인이 보여도 해당 모션을 끝내고 3초 정지한다. 정지가 끝난
뒤 새 라인 영상에서 정렬이 확인되어야 기본 라인 주행으로 넘어간다.
영상이 없거나 오래된 경우에는 추가 회전 없이 기다린다.

정렬 완료 시 `LINE_TRACK_AFTER_PICKUP`으로 들어가고 Ball mode를 종료한다.
이 구간은 기본 라인 제어기의 전진·방향 보정·라인 분실 탐색을 사용한다.
단, 이 10초 구간에서는 앞쪽 코너의 `turn_angle_deg`·`path_turn_delta_deg`를
조향 판단에 사용하지 않고, 고정 코너 회전용 `LEFT`·`RIGHT` 명령을 차단한다.
현재 라인의 heading·좌우 위치에 따른 기존 `RECOVER_*` 복귀 보정은 유지한다.
라인을 놓치면 기존 `LINE_LOST_TURN_*` 탐색도 유지하므로 모든 회전을 금지하는
설정은 아니다. 모션 중 수집한 영상 재평가에도 같은 제한을 적용한다.
이 구간 밖의 일반 라인 주행과 앞선 픽업 후 라인 정렬 기준은 바꾸지 않는다.
일반 직진 `STRAIGHT`는 기존 `line_forward_6`/`전진실험45도(6회)`를 사용하며,
한 번의 고정 전진 시퀀스로 묶지 않는다. 새 공이나 골대 검출은 주행을 끊지 않는다.
10초는 첫 라인 모션의 `RUNNING` 수신부터 센 경과 시간이며, 이후 보정과
영상 대기도 포함한다. 10초가 지나면 현재 모션을 정상 종료한 뒤 잡기 결과로 분기한다.
이 시간 경계를 확인할 수 있도록 해당 구간에서는 다음 직진을 미리 예약하지 않는다.
라인 모션이 실패·취소·시간 초과되면 추가 주행과 카메라 전환을 중단한다.
공 집기 후 라인 정렬은 기존대로 라인의 `filtered_heading_error_deg`를 사용하고,
없으면 `heading_error_deg`를 사용한다. 좌우 위치 오차나 가까운 라인 목표점으로
회전 기준을 전환하지 않는다. 우측 오차 15° 미만·좌측 오차 30° 미만이면 완료하며,
그 이상이면 현재 총 회전각 표에서 오차를 넘지 않는 모션을 선택한다.
횡방향 위치는 진단값으로만 기록하므로, 기울기가 맞아도 로봇이 라인 위에 있다는
뜻은 아니다. 실제 라인으로 진입하는 경로는 별도로 확인해야 한다.
관절 위치 허용 오차 5°와는 별개다. 영상 기반 판단이므로 모의 입력/시뮬레이션
확인 후 지지 상태에서 최소 회전량의 과보정 여부를 확인해야 한다.
10초 후 해당 공의 저장된 잡기 결과가 `GRABBED`이면 `POST_BALL_GOAL_TRANSITION`에서
`오뒤카메라90도`를 실행한다. 카메라 동작 시간을 포함해 최소 3초 정지하며,
카메라 동작이 3초보다 길면 성공할 때까지 기다린다. 이후 기존 골대 영상을
폐기하고 새 영상으로 `GOAL_APPROACH`를 시작한다. 추가 라인 재정렬이나
별도의 3초 정지는 넣지 않는다. 카메라 모션 실패·시간 초과 시에는 접근하지 않는다.
잡기 결과가 `NOT_GRABBED` 또는 `UNKNOWN`이면 카메라를 들지 않고 라인 주행을
이어간다. 이 공 구간은 `ball_sections_processed`에만 통과 처리하며
`shots_completed`는 올리지 않는다. `AUTO`에서 다음 공을 찾을 수 있고,
모든 공 구간을 통과했다면 `LINE_TRACK`을 유지한다. 다음 픽업 전에는 골대
검출이 주행을 가로채지 못한다. 다음 슛은 건너뛴 공이 아닌 해당 공의 잡기 결과를 쓴다.
`mission_progress.ball_mode_active`는 공 접근·픽업 시퀀스·픽업 후 라인 정렬까지만
`true`다. 라인 정렬 완료 이후에는 `false`다. 후진·제자리 회전은 픽업 시퀀스
내부이며, 라인 정렬과 10초 라인 주행은 픽업 시퀀스 이후 단계다.
`pickups_completed`는 잡기 시퀀스 완료 횟수이므로 ball mode 종료와 별도로 유지한다.
카메라 전환 실행 중에는 같은 전환 명령을 다시 발행하지 않는다.

픽업 완료부터 라인 정렬·10초 라인 주행과, 잡기 성공 시 골대 접근·슛 후 라인 복귀까지는
새 공 검출로 공 접근이나 탐색을 시작하지 않는다. 단계 전환 시 남은 공 접근·잡기
예약, 검출 확정 대기, 마지막 공 위치·탐색 방향과 공 접근용 정지 시간을 정리한다.
실행 중인 모션의 잠금, 라인·골대의 정지 시간, 잡기 판정 결과와 완료 횟수는 유지한다.
슛 후 복귀 전진이 성공하면 이전 공·골대 영상을 폐기하고 완료한 골대의 제어 잠금과
추적도 정리한다. `AUTO`로 복귀한 뒤 새 영상에서 다음 공 탐색을 재개한다.
잡기 전 공을 놓쳤을 때의 기존 탐색은 그대로 유지한다.

픽업 중 마지막으로 완료한 실제 모션이 `전진0도-1(4회)`이고 다음 요청이
`미세왼옆꽃게0도-1` 또는 `미세오옆꽃게0도`이면, 꽃게걸음 직전에
`pickup_crab_prepare`를 1회 실행한다. 3초 대기와 영상 판단은 실제 모션 이력에
포함하지 않는다. 준비 자세 성공 직후 요청했던 꽃게걸음을 실행하고,
꽃게걸음 완료 후 기존 3초 대기·정렬 판단으로 돌아간다. 준비 자세가 실패하거나
시간 초과·취소되면 꽃게걸음으로 진행하지 않는다. 중간에 미세전진·회전을 했거나
연속 꽃게걸음을 요청하는 경우에는 준비 자세를 추가하지 않는다.

`pickup_crab_prepare`는 `오뒤에서 기본자세(카메라45도)`를 복사한
`오뒤에서 기본자세(픽업 카메라0도)`에 연결한다. 모터 0의 프레임 각도만 기존
`전진0도-1(4회)`와 같은 -60°로 맞추고, 몸 관절·토크·시간·속도는 유지한다.
기존 카메라 override는 일반 접근 모션 중 공의 하단 진입 조건으로 활성화되므로
픽업 진입만으로 항상 켜지는 것은 아니다. 활성화되어 있으면 잡기 직전까지
유지되지만, 이 준비 모션은 override가 꺼져 있어도 자체 데이터로 카메라를
아래에 유지한다. 완료 허용 오차는 5°다. 실물 적용 전 시뮬레이션과 로봇 지지 상태에서
전진 종료 자세 → 기본자세 → 꽃게걸음 연결을 확인해야 한다.

일반 BALL 접근은 거리와 무관하게 `STRAIGHT_2` → `전진실험45도(4회)`를
사용한다. 카메라를 내린 pickup 초기 정렬 후에는 유효하고 최신인
`distance_m`으로 전진 동작을 선택한다.

- `pickup_fine_step_distance_m` 기본값 `0.570`: 570 mm 초과이면
  `STRAIGHT_2` → `전진0도-1(4회)`, 이하이면 `STRAIGHT_0` → `미세0도-4`다.
  거리값이 없거나 0 이하, `depth_valid=false`, `depth_age_sec`가 없거나
  0~0.70초 범위를 벗어나면 전진을 대기한다.
- `pickup_fine_align_bottom_distance_px` 기본값 `300`: 미세실전 완료 후
  새 영상의 `bottom_distance_px`가 이 기준보다 크면 미세실전을 반복한다.
  이하이면 꽃게걸음으로
  좌우 정렬하고, `offset_x_px`가 `-30~+55` 범위에 들어오면 추가 미세전진 없이
  공잡기 전 후진 단계로 넘어간다.

매 동작 후 기존 3초 대기와 새 영상 확인을 거친다. 픽셀 간격이 유효하지
않으면 대기한다. `bottom_distance_px`는 화면 하단과 공 중심 사이 픽셀 간격이며,
근접 게걸음 및 미세전진 이후 정렬에 계속 사용한다. 두 기준은 독립적이며
두 full-system launch에서 인자로 조정할 수 있다. 기존
`pickup_fine_step_bottom_distance_px` 인자는 미터 단위의
`pickup_fine_step_distance_m`으로 교체했다. 픽셀 기준은 카메라 각도와 해상도에 영향을
받으므로 반복 접근은 먼저 모의 입력으로 확인하고 실물 이동량을 검증해야 한다.

`enable_ball_lost_recovery` 기본값은 `true`다. 유효한 거리로 1.5m 이내에서
확인한 공이 영상에서 사라지면, 화면 중앙 기준 `camera_center_offset_x_px`의
부호로 좌우 탐색 방향을 정한다. 음수는 왼쪽, 양수는 오른쪽이며 정확히
중앙이면 이전 방향을 유지한다. 추적 시작은 거리와 확정 검출 조건을 유지하되,
추적 중에는 회전 중의 확정 전 raw 검출도 신뢰도 기준을 만족하면 마지막
화면 방향을 갱신한다. 따라서 왼쪽으로 돌다가 공이 화면 오른쪽에서 마지막으로
보이면 다음 분실 탐색은 오른쪽이다. 라인 위치와 로봇 중심 보정값은 사용하지 않는다.
일반 접근에서는 `제자리좌회전45도-1(2회)` 또는 `제자리우회전45도-1(5회)`를
사용한다. 픽업 전 초기·미세 정렬에서는 카메라를 내린 좌회전/우회전
각각 2회/5회를 사용하며, 완료·3초 대기·새 영상 확인 후 필요하면 반복한다.
단, 잡기 전 픽업 단계에서 신뢰도 기준을 만족하는 공의 `bottom_distance_px`가
100px 이하로 한 번 확인되면 해당 픽업은 근접 정렬 상태를 유지한다.
모션 실행·대기 중의 raw 검출도 이 근접 상태를 기록한다. 이후 픽셀값이 100px를
넘어도 공이 보이는 동안은 제자리 회전을 하지 않고 미세전진·꽃게걸음으로 정렬한다.
공을 놓친 경우에는 근접 상태에서도 기존 공 탐색을 적용한다. 마지막 화면 방향에
따른 좌회전/우회전 2회/5회 또는 아래 상단 이탈 조건의 1회 전진 탐색을 사용한다.
근접 상태만을 이유로 재검출까지 대기하지는 않는다. 새 영상이 없거나 raw 검출이
아직 확정되지 않았거나 탐색에 필요한 추적 이력이 없는 경우의 대기는 유지한다.
새 픽업 시작과 픽업 종료 시 근접 상태를 초기화하며, 잡기 후 고정 회전과 라인
복귀 회전에는 적용하지 않는다.
마지막 공 검출 상자의 위쪽 끝이 화면 상단 10% 이내에 있고 이후
`detected=false`, `raw_detected=false`가 되면 좌우 탐색보다
`전진0도-1(2회)`를 우선 실행한다. 720px 높이에서는 상단 72px까지이며,
`MotionDecisionPlanner.BALL_LOST_TOP_EDGE_RATIO`에서 기준을 조정한다.
이 조건은 상단 이탈의 영상 기반 추정이며, 일반 접근과 픽업 초기·미세 정렬에
적용한다. 높이/검출 상자가 없거나 유효하지 않으면 상단 이탈로 판단하지 않는다.
한 번의 공 분실에 전진 모션은 한 번만 실행하고, 3초 정지 후 새 영상에서도
계속 LOST이면 기존 좌우 탐색을 사용한다. 신뢰도 기준을 만족하는 공을 다시
관측하면 새 위치로 판단을 갱신한다. 잡기 고정 시퀀스가 시작된 뒤에는 적용하지
않는다. 새 전진 분기는 모의 입력/시뮬레이션에서 먼저 검증해야 한다.

진행 중인 모션을 중단하지 않으며, 새 프레임이 없거나 raw 검출의 확정을
기다리는 동안에는 새 탐색 회전을 시작하지 않는다. 실제 잡기 순서가 시작된
후에는 이 탐색을 적용하지 않는다. YOLO 상단에는 분실 중 `BALL LOST`와
마지막 탐색 방향을 표시하며, 모터 오류 표시는 그보다 우선한다.

공 접근·픽업 초기 정렬·후진 후 재정렬은 Path angle(`steering_angle_deg`)을
우선 사용한다. 라인 재정렬은 heading, 골대 정렬은 bearing을 사용하며,
공·라인 정렬은 다음 공통 표를 사용한다. 골대는 우회전만 이 표를 사용하고,
좌회전에는 아래 별도 기준을 적용한다.

| 좌측 각도 오차 | 좌회전 횟수 | 우측 각도 오차 | 우회전 횟수 |
| --- | --- | --- | --- |
| 30° 미만 | 회전 없음 | 15° 미만 | 회전 없음 |
| 30° 이상 ~ 45° 미만 | 1회 | 15° 이상 ~ 30° 미만 | 2회 |
| 45° 이상 ~ 60° 미만 | 2회 | 30° 이상 ~ 45° 미만 | 3회 |
| 60° 이상 ~ 75° 미만 | 3회 | 45° 이상 ~ 65° 미만 | 5회 |
| 75° 이상 ~ 90° 미만 | 4회 | 65° 이상 ~ 95° 미만 | 7회 |
| 90° 이상 | 5회 | 95° 이상 | 9회 |

골대 좌회전은 오차 15° 이하에서는 생략하고, 15° 초과~45° 이하에서는 1회,
45° 초과~60° 미만에서는 2회, 60° 이상~75° 미만에서는 3회,
75° 이상~90° 미만에서는 4회, 90° 이상에서는 5회를 선택한다.
15° 경계는 회전 없음, 45° 경계는 1회다. 이는 선택 범위 변경이며
좌회전 1회의 보정각은 계속 약 30°이므로 15~30° 잔여 오차에서는
목표 방향을 지나칠 수 있다. 모의 입력 검증 후 실물은 지지 상태에서 확인한다.
골대 미검출 시 탐색과 픽업·슛 이후의 고정 회전에는 이 범위를 적용하지 않는다.

일반 공 접근·라인 재정렬은 카메라45도용, 픽업 정렬은 카메라0도용,
골대 정렬은 카메라90도용 모션에 연결한다. 회전 데이터는 최신
`Downloads/robot_motions(13).json`을 실행 카탈로그에 그대로 반영했다.
좌회전 1~5회는 보정각 30°·45°·60°·75°·90°이며, 우회전 2·3·5·7·9회는
15°·30°·45°·65°·95°다. 양쪽 모두 `turn_repeat_deg`는 `null`이고
`turn_angle_deg`는 해당 보정각이다. 6회 좌회전 데이터와 기존 별칭은 유지하지만
이 각도 기반 선택에서는 사용하지 않는다.
공 초기 정렬에서 픽셀 오차가 ±70px를 넘으면 픽셀 부호로 방향을 선택한다.
가까운 공의 게걸음 정렬과 Path angle의 근거리 중앙 deadband는 유지한다.
골대 득점 거리에서의 게걸음 정렬도 유지한다. 각도 기준 아래에서는 제자리
회전을 건너뛰므로 기존보다 큰 잔여 오차 상태로 다음 전진/정렬 단계에 진입할 수 있다.

공 분실 탐색은 고정 좌2회/우5회(양쪽 보정각 약 45°)이며, 픽업 뒤 회전은 첫 공 우9회/둘째 공 좌3회다.
일반 라인 `RIGHT`는 `line_turn_right_large` → `제자리우회전45도(9회)`로 연결한다.
`line_turn_right_2/3/5/7/9`도 각각 같은 횟수의 카메라45도 제자리우회전으로 연결한다.
관절 프레임은 변경하지 않았다.
`full_system_robot.launch.py`의 초기 자세는 `오뒤412`이고 전환 시간은 기존 4초다.
변경된 회전량과 분실 탐색은 모의 입력 및 지지대 실험 후 자유 보행에 적용한다.
현재 모션 데이터는 `robot_motions(12).json` 원본과 동일하다.
45도 자세의 9회 우회전만 원본 이름이 `제자리우회전45도(9회)`이며 `-1`이 없다.

## 토픽 인터페이스

| 토픽 | Publisher | Subscriber | 타입 | 역할 |
|---|---|---|---|---|
| `/navigation/motion_command` | `motion_decision_node` | `motion_command_bridge_node` | `std_msgs/msg/String` | action, command/event ID와 source 정보를 전달한다. |
| `/motion/executor/request` | `motion_command_bridge_node` | `sdk_motion_executor` | `std_msgs/msg/String` | 검증된 motion alias와 `request_id`를 JSON 요청으로 전달한다. |
| `/motion/executor/cancel` | 향후 cancel 요청자; 현재 공식 bridge는 미발행 | `sdk_motion_executor` | `std_msgs/msg/String` | 활성 `request_id`의 취소 요청을 전달한다. |
| `/motion/executor/status` | `sdk_motion_executor` | `motion_command_bridge_node` | `std_msgs/msg/String` | RUNNING 및 terminal 실행 상태와 오류를 반환한다. |
| `/motion/status` | `motion_command_bridge_node` | `motion_decision_node` | `std_msgs/msg/String` | navigation 계층에 action 상관관계가 포함된 상태를 반환한다. |

## Deprecated executable

다음 entry point는 이전 테스트와 전환 작업을 위해 아직 설치되지만 공식 실행
경로가 아니며 사용하면 안 된다. 파일과 entry point는 이번 단계에서 삭제하지
않는다.

- `motion_executor_node`: legacy Python executor
- `legacy_motion_executor_adapter`
- `legacy_motion_status_adapter`
- `sdk_motion_stub_node`

특히 Python `motion_executor_node`와 C++ `sdk_motion_executor`를 동시에 실행하면
동일 executor 토픽을 두 구현이 소유할 수 있으므로 금지한다. 공식 launch 두 개는
위 deprecated executable을 하나도 실행하지 않는다.

## 빌드

```bash
cd ~/IRC/IRC-STEP/IRC_vision_latest
source /opt/ros/humble/setup.bash
colcon build --packages-select step irc_step_motion_executor mission_control \
  --symlink-install
source install/setup.bash
```

## 카메라 좌우 영역

라인 분석의 좌우 ROI 기본값은 `0.0~1.0`으로 전체 폭을 사용한다.
기존 좌우 15% 제외를 해제했으며, 단독 분석 노드와 두 full-system launch에
동일하게 적용했다. 화면 가장자리의 라인도 추종 판단에 포함된다.

## 라인 복귀 매핑과 소실 탐색 (2026-09-20)

`RECOVER_<라인 위치>_TURN_<회전 방향>_<n>`에서 회전 방향과 반복 횟수를 그대로
실제 모션에 적용한다. 첫 번째 LEFT/RIGHT는 라인의 위치이며 모션 계열을 바꾸지 않는다.

| 명령 | 실행 모션 |
| --- | --- |
| `RECOVER_LEFT_TURN_LEFT_n`, `RECOVER_RIGHT_TURN_LEFT_n` | `라인복귀좌회전(n회)` |
| `RECOVER_LEFT_TURN_RIGHT_n`, `RECOVER_RIGHT_TURN_RIGHT_n` | `라인복귀우회전(n회)` |
| `LINE_LOST_TURN_LEFT` | `제자리좌회전45도-1(1회)` |
| `LINE_LOST_TURN_RIGHT` | `제자리우회전45도-1(3회)` |

좌회전 반복 수는 2/3/4/5/6/7/8/10/13회, 우회전 반복 수는
2/3/4/5/6/7/8/10/12/15회가 등록되어 있다. 각도에서 n을 선택하는 기존 기준은 유지한다.
복귀 보행 데이터는 `Downloads/robot_motions-1(3).json`을 기준으로 갱신했다.
앞으로 모션 변경 시에도 사용자가 가장 최근에 제공한 JSON을 기준으로 데이터와
매핑을 수정한다. 파일 수정 시간이 아니라 사용자 제공 순서로 기준 파일을 정한다.
이 라인복귀 데이터의 출처는 `/home/jet/Downloads/robot_motions-1(3).json`이다.
이후 제공된 `robot_motions(15).json`에서는 사용자가 지정한 공잡기·후진·오른쪽 꽃게
모션만 갱신했다. 전체 카탈로그를 첨부 파일로 교체하지 않는다.
8회 모션의 원본 오타 `라인보귀…(8번)`는 새 표준 이름으로 등록했다.
원본에 없는 우7회 및 큰 반복 수는 같은 방향 4회 모션의 주기를 사용해
`repeat_count`만 변경했다. 완료 허용 오차 5°는 유지한다.

### 골넣기 완료 후 라인 복귀

`SHOT`의 정상 완료 상태를 받은 뒤 다음 순서로 실행한다.

1. `POST_SHOT_TURN`: 3초 정지 후 첫 번째 골대는 `기본자세에서 제자리 우회전(골대)`,
   두 번째 골대는 `기본자세에서 제자리좌회전(골대)`를 각각 1회 실행한다.
2. `POST_SHOT_LINE_ALIGN`: 고정 회전 완료 후 3초 정지하고 새 라인 관측으로 방향을 확인한다.
   이미 정렬되어 있으면 추가 회전을 생략한다. 보정이 필요하면 기존 총 회전각 표를
   사용해 우 2/3/5/7/9회 또는 좌 1/2/3/4/5회를 선택한다. 추가 보정·탐색 회전도
   매번 정상 완료 수신 시점부터 3초 정지한 뒤 새 관측으로 재확인한다.
   정지 중 관측은 폐기하고, 중복 완료 통지는 대기 시간을 연장하지 않는다.
   이 단계에서는 RECOVER 보행을 사용하지 않는다.
3. `POST_SHOT_FORWARD`: 회전 후 3초 정지를 마친 새 영상에서 정렬이 확인되면
   기본 전진 `전진실험45도(6회)`를 한 번 실행한다. 이미 마친 정지에 3초를 추가하지 않는다.
   방향이 맞지 않으면 다시 회전하고 3초 정지한다. 회전 이력 없이 정렬 단계에 직접
   진입한 경우에는 전진 전 3초 대기를 적용한다.
   전진 성공 후 첫 번째 공은 `AUTO`, 마지막 공은 `LINE_TRACK`으로 돌아간다.

정렬 기준은 기존 공 집기 후 라인 정렬과 동일하다. 오른쪽 오차 15° 미만,
왼쪽 오차 30° 미만은 현재 최소 회전량보다 작으므로 추가 회전을 생략한다.
횡방향 위치 오차는 이 단계의 회전 조건으로 사용하지 않는다. 유효한 영상이 없거나
품질이 낮으면 대기하고, 새 영상에서 라인이 미검출되면 마지막 회전 방향으로 최소 횟수
제자리회전을 하며 탐색한다. 회전·전진 실패 및 시간 초과 시에는 해당 단계에서 정지한다.

골대 복합 모션도 `Downloads/robot_motions(19).json`의 원본 데이터를 사용한다.
내부 회전은 첫 번째 우회전 7회, 두 번째 좌회전 4회이며, 자세 전환도 포함되어 있다.
기존 액션 ID `POST_SHOT_TURN_RIGHT_9` / `POST_SHOT_TURN_LEFT_4`는 호환성을 위해
유지하지만 실제 실행은 `post_shot_default_turn_right` / `post_shot_default_turn_left`
별칭으로 위 복합 모션을 호출한다.
실기 적용 전 시뮬레이션에서 골넣기 종료 자세와 회전 시작 자세의 연결을 확인하고,
지지 상태에서 회전량과 전진을 검증해야 한다.

일반 라인 주행(AUTO, LINE_TRACK 및 공/골대 탐색 중 라인 주행)에서 정상 검출한
가장 가까운 라인 점의 좌우 방향을 기억한다. 보행 중 수신한 영상도 갱신에 사용하며,
원시 검출점 좌표가 없는 입력은 lateral offset으로 판단한다. 라인을 새 영상에서
미검출하면 기억한 방향의 제자리 회전을 한 번 실행한다. 회전이 완료되어야 다음
명령을 허용하고, 회전 중 받은 라인 정보는 폐기한 뒤 새 영상으로 재판단한다.
다시 보이면 정상 주행으로 복귀하고, 여전히 미검출이면 같은 절차로 탐색한다.

관측 누락·시간 초과·기억한 방향 없음은 정지한다. 공/골대/허들 단계가 선택되면
일반 라인 방향 기억을 지우며 해당 미션의 우선순위를 유지한다. 공 집기 직후의
`POST_BALL_LINE_ALIGN`은 기존 전용 정렬 절차를 사용한다. 라인이 사라졌을 때는
이전 라인 형상으로 준비한 RECOVER 명령보다 소실 탐색을 우선하며, 오래된 검출
프레임만으로 다음 STRAIGHT를 미리 예약하지 않는다.

재매핑으로 실제 보행 주기와 반복 수가 달라졌으므로, 기존 각도 추정값과 실제
회전량이 일치하는지는 시뮬레이션 후 지지 상태에서 확인해야 한다. 특히 오른쪽
소실 탐색은 `제자리우회전45도-1(3회)`이며 종료 자세가 `오뒤415`이다.

## YOLO 실행 모션 표시

YOLO 상단 배너는 `/motion/status`의 `RUNNING` 상태를 받아 실제 실행 모션을 표시한다.
판단 명령이 `PICKUP_NOW` 하나여도 준비 자세·미세전진·꽃게·후진·잡기·공 확인·회전을
각각 구분한다. 라인 복귀, 제자리 회전, 골대 후진과 미세전진도 표시하며,
OpenCV 기본 글꼴에서 읽을 수 있도록 영문 모션 이름을 사용한다.
실행 표시를 판단 배너 위에 그리되 기존 오류·공 분실·잡기 확인 표시는 우선 유지한다.
관련 명령 종료 시 실행 표시를 지우고, 종료 메시지가 누락돼도 120초 뒤에는 만료한다.
